# 接入「模拟面试」（ai-interview2 → admin-hub）

> 2026-05-27 启动 / 跑 admin-hub-add-project skill

## 目标

把 ai-interview2 项目接入 admin-hub「报告管理」侧边栏，成为第 7 个业务。
列表字段：时间 / 姓名 / 手机号 / 服务项目 / 面试岗位 / 岗位职级 / 面试语言 / 企业性质 / 操作。
操作列：报告、档案。不支持转服务。接入完直接部署。

## 已拍板决策（Step 0）

- 路径：`D:\_workspace\01_项目-Coding\ai-interview2\`
- 参照模板：startup（schema 几乎一样）
- 转服务：❌ 不支持（不动 service_tracking）
- 部署：✅ 接入完后直接 tencent-deploy

## 待拍板决策（Phase 2 输出对照表后用户确认）

- `ProjectId` 短英文 key（候选：`interview`）
- 侧边栏 label / shortLabel / 图标 / 配色（globals.css 现已用 blue/green/purple/orange/cyan/amber 六色，第 7 个需加色阶变量）
- env 变量名（候选：`INTERVIEW_DB_PATH`）
- 列表 5 个面试专属字段在 db 里的实际取数路径

## Phase 拆解

1. 双向探索（Explore agent A + B 并行）—— 摸 ai-interview2 数据 + startup 实现
2. 字段对照表 + 命名建议 → 用户确认
3. 类型 + projects.ts + db.ts + sidebar（基础注册）
4. 列表 API + 详情 API + resume API（数据层）
5. 复制报告渲染子组件 + admin renderer（components/report/interview/、components/admin/interview-report-renderer.tsx）
6. 列表页 + 详情页 + 预览页（UI 层）
7. globals.css 加新色阶 + PageHeader 类型扩
8. 本地验证（tsc + eslint + build + DB 连通性 + dev server UI smoke）
9. tencent-deploy 部署 + 服务器 .env 加 INTERVIEW_DB_PATH

## 已知风险

- ai-interview2 的 db 文件名叫 `startup-diagnostic.db`（历史包袱），ATTACH alias 要小心别撞 startup
- form_data_json 实际结构未知，Phase 1 必须拿到真实样本
- 部署后服务器 .env 要手动加 INTERVIEW_DB_PATH，且 ai-interview2 项目本身得先部到服务器上、db 文件得存在
- 「模拟面试」业务在管理后台是第 7 个项目，sidebar 已经偏长，要看 UI 是否还撑得住

## 验收点（每 phase 完成后回写 progress）

- Phase 1：产出探索结论（贴 ai-interview2 字段 + startup 关键文件路径）
- Phase 2：用户回 yes 才进 Phase 3
- Phase 3-7：每 phase 单独跑 `npx tsc --noEmit` 不报错
- Phase 8：`rm -rf .next && npm run build` 全绿、dev server 手工跑通侧边栏 → 列表 → 详情
- Phase 9：curl 生产 `/b100/admin/reports?project=interview` 200 + 列表数据正常

---

## Progress（执行中 append）

- **2026-05-27** Phase 1 完成：双向探索得 ai-interview2 db 可读、reports 表 schema 与 startup 一致（form/quiz/scoring/interview_q1q2/report 全 5 个 JSON 列）、8 个列表字段中 7 个能定位到、第 8 个「服务项目」是固定值
- **2026-05-27** Phase 2 完成：命名 = interview / 模拟面试 / 面试 / rose / Mic / sectionCount=3 / INTERVIEW_DB_PATH，用户全部默认推荐通过
- **2026-05-27** Phase 3 完成：新建 lib/types-interview.ts（含 form-options labels）+ projects.ts 加 7 项 + db.ts 加 ATTACH/isReady/dbDir + admin-sidebar.tsx 加图标
- **2026-05-27** Phase 4 完成：reports/route.ts 加 interviewSelect/conditions/statForProject/降级 + [id]/route.ts tableMap + resume API 同步加（避免 plan 容易漏的项）
- **2026-05-27** Phase 5 完成：复制 5 个核心 section 组件到 components/report/interview/（report-context / section-wrapper / interview-score / job-matching / psych-interpretation），sed 替换 import 指向 types-interview；新建 components/admin/interview-report-renderer.tsx 渲染 3 模块；删 4 个未用备份避免 lint error
- **2026-05-27** Phase 6 完成：列表页 14 处分支（columns/KPI/筛选/RowItem/Badge/PageHeader/3 个枚举下拉），详情页 13 处分支（基本信息/量表/访谈/3 模块报告），预览页 1 处分支
- **2026-05-27** Phase 7 完成：globals.css :root + .dark 加 rose 6 阶色阶（hue 18，仿 cyan/amber 结构），PageHeader AccentColor 类型扩 rose + ICON_BG 加 rose 项
- **2026-05-27** Phase 8 完成：tsc clean / eslint 0 errors（5 unused-var warnings 与本次接入主要无关）/ npm run build clean / dev server 启动 ✓ / `/api/admin/reports?project=interview` 401 表示 parseProject 收 interview 入参合法 / .env.local 加 INTERVIEW_DB_PATH 指向 ai-interview2/data/startup-diagnostic.db（db 可读，reports 表存在但 0 条记录 = 预期）
